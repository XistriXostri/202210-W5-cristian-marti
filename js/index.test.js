// Lo que había hecho
// import { arrayLength } from './index.js';

// const arrayL = ['a', 'b', 'c'];

// describe('Given arrayLength function', () => {
//     test(`The array lenght of ${arrayL} should be ${arrayL.length}`, (array) => {
//         const result = arrayLength(arrayL);
//         expect(result).toBe(arrayL.length);
//     });
// });
